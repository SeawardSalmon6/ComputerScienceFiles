import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  userRow: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
  },
  userName: {
    fontSize: 20,
    fontWeight: "600",
    paddingHorizontal: 10,
    paddingVertical: 5,
  },
});
